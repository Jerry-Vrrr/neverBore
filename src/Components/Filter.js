
const Filter = () => {
  
  return (
    <section className="filter">
      <button>About</button>
      <button>Relaxation</button>
      <button>Contact</button>
    </section>
  );
};

export default Filter;
